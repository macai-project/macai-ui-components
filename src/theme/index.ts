import { createTheme } from "@mui/material"
import * as typography from "./typography"
import * as palette from "./palette"
import * as components from "./components"

export const warehouseMobileTheme = createTheme({
  palette,
  typography,
  components,
})
